# ============================================
# Smart Fashion API - Environment Configuration
# ============================================
# Copy this file to .env and customize values as needed
# cp .env.example .env

# ============================================
# Application Settings
# ============================================
APP_VERSION=2.0.0
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000
UVICORN_WORKERS=1
OMP_NUM_THREADS=4

# ============================================
# Model Settings
# ============================================
# ONNX model key in MinIO bucket (must upload model to MinIO first)
MINIO_MODEL_KEY=deepfashion2_yolov8s-seg.onnx

# ============================================
# MariaDB Database
# ============================================
DB_HOST=mariadb
DB_PORT=3306
DB_NAME=smartfashion
DB_USER=smartfashion
DB_PASSWORD=smartfashion
DB_ROOT_PASSWORD=rootpassword

# ============================================
# MinIO Object Storage
# ============================================
# Credentials (REQUIRED - change in production!)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=admin123456

# Bucket settings
MINIO_BUCKET=smartfashion
MINIO_REGION=us-east-1
MINIO_SECURE=false

# Internal endpoint (used by app container to connect to MinIO)
# Note: This is overridden by compose.yml to use docker network hostname
MINIO_ENDPOINT=http://localhost:9000

# External endpoint (used for image URLs accessible from browser)
# IMPORTANT: For network access from other machines, set this to your server's IP!
# Examples:
#   - Local only: http://localhost:9000
#   - Network access: http://192.168.1.100:9000 (replace with your server IP)
#   - Production: https://minio.yourdomain.com
MINIO_EXTERNAL_ENDPOINT=http://localhost:9000
